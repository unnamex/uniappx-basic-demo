<template>
	<view class="resource-preview">
		<!-- 无资源状态 -->
		<view v-if="resType == ''" class="empty-state">
			<text class="empty-icon">📂</text>
			<text class="empty-text">选择资源进行预览</text>
		</view>
		
		<!-- 图片预览 -->
		<view v-else-if="resType == 'image'" class="preview-container image-preview">
			<image 
				class="preview-image"
				:src="resPath"
				mode="aspectFit"
				@error="onImageError"
			/>
			<view class="resource-info">
				<text class="resource-name">{{ resName }}</text>
				<text v-if="resDescription != ''" class="resource-desc">{{ resDescription }}</text>
			</view>
		</view>
		
		<!-- 视频预览 -->
		<view v-else-if="resType == 'video'" class="preview-container video-preview">
			<video
				class="preview-video"
				:src="resPath"
				:poster="resThumbnail"
				controls
				:autoplay="false"
				object-fit="contain"
				@error="onVideoError"
				@timeupdate="onTimeUpdate"
				@ended="onEnded"
			/>
			<view class="resource-info">
				<text class="resource-name">{{ resName }}</text>
				<text v-if="resDuration > 0" class="resource-duration">时长: {{ formatTime(resDuration) }}</text>
			</view>
		</view>
		
		<!-- 音频预览 -->
		<view v-else-if="resType == 'audio'" class="preview-container audio-preview">
			<view class="audio-visual">
				<text class="audio-icon">🎵</text>
				<view class="audio-wave">
					<view v-for="i in 5" :key="i" class="wave-bar" :class="{ 'playing': isPlaying }"></view>
				</view>
			</view>
			<view class="audio-controls">
				<button class="audio-btn" @click="toggleAudio">
					<text>{{ isPlaying ? '⏸️' : '▶️' }}</text>
				</button>
				<view class="audio-progress">
					<view class="progress-bar">
						<view class="progress-fill" :style="{ width: progressPercent + '%' }"></view>
					</view>
					<text class="time-text">{{ formatTime(currentTime) }} / {{ formatTime(resDuration) }}</text>
				</view>
			</view>
			<view class="resource-info">
				<text class="resource-name">{{ resName }}</text>
			</view>
		</view>
		
		<!-- PDF预览 -->
		<view v-else-if="resType == 'document' && isPdf()" class="preview-container pdf-preview">
			<!-- #ifdef WEB -->
			<view ref="pdfFrame" class="pdf-frame-container"></view>
			<view class="resource-info">
				<text class="resource-name">{{ resName }}</text>
			</view>
			<!-- #endif -->
			<!-- #ifdef APP -->
			<!-- #ifdef APP-HARMONY -->
			<KuxHarmonyPdf :src="resPath" class="pdf-native-view"></KuxHarmonyPdf>
			<!-- #endif -->
			<!-- #ifndef APP-HARMONY -->
			<web-view :id="webviewId" class="pdf-webview" :src="pdfWebViewSrc" @load="onPdfWebViewLoad"></web-view>
			<!-- #endif -->
			<view class="resource-info">
				<text class="resource-name">{{ resName }}</text>
			</view>
			<!-- #endif -->
		</view>
		
		<!-- CAD 图纸预览 -->
		<view v-else-if="resType == 'cad'" class="cad-preview-container">
			<!-- #ifdef WEB -->
			<view ref="cadFrame" class="cad-frame-container"></view>
			<view class="cad-info">
				<text class="cad-info-name">{{ resName }}</text>
			</view>
			<!-- #endif -->
			<!-- #ifdef APP -->
			<web-view :id="cadWebviewId" class="cad-webview" :src="cadWebViewSrc" @load="onCadWebViewLoad"></web-view>
			<view class="cad-info">
				<text class="cad-info-name">{{ resName }}</text>
			</view>
			<!-- #endif -->
		</view>
		
		<!-- 其他文档预览 -->
		<view v-else-if="resType == 'document'" class="preview-container pdf-preview">
			<!-- #ifdef WEB -->
			<view class="pdf-frame-container" ref="docFrame"></view>
			<!-- #endif -->
			<!-- #ifndef WEB -->
			<web-view 
				class="pdf-webview" 
				:id="docWebviewId" 
				:src="docWebViewSrc" 
				@loaded="onDocWebViewLoad"
				@error="onVideoError"
			></web-view>
			<!-- #endif -->
		</view>
		
		<!-- 未知类型 -->
		<view v-else class="preview-container unknown-preview">
			<text class="unknown-icon">❓</text>
			<text class="unknown-text">不支持预览此类型文件</text>
			<text class="resource-name">{{ resName }}</text>
		</view>
	</view>
</template>

<script setup lang="uts">
	import KuxHarmonyPdf from '@/uni_modules/kux-harmony-pdf/components/kux-harmony-pdf/kux-harmony-pdf.uvue'
	import { ResourceItem, formatDuration } from '@/types/resource'
	
	// Props
	const props = defineProps<{
		resource: ResourceItem | null
	}>()
	
	// Emits
	const emit = defineEmits<{
		(e: 'error', message: string): void
		(e: 'loaded'): void
	}>()
	
	// 音频播放状态
	
	const resType = computed((): string => props.resource?.type ?? '')
	const resPath = computed((): string => props.resource?.path ?? '')
	const resName = computed((): string => {
		const name = props.resource?.name ?? ''
		const lastDotIndex = name.lastIndexOf('.')
		if (lastDotIndex > 0) {
			return name.substring(0, lastDotIndex)
		}
		return name
	})
	const resDuration = computed((): number => props.resource?.duration ?? 0)
	const resThumbnail = computed((): string => props.resource?.thumbnail ?? '')
	const resDescription = computed((): string => props.resource?.description ?? '')
	const resId = computed((): string => props.resource?.id ?? '')
	
	// #ifdef APP
	const webviewId = 'pdf_' + Date.now() + '_' + Math.floor(Math.random() * 10000)
	const cadWebviewId = 'cad_' + Date.now() + '_' + Math.floor(Math.random() * 10000)
	const docWebviewId = 'doc_' + Date.now() + '_' + Math.floor(Math.random() * 10000)

	const pdfWebViewSrc = computed((): string => {
		const res = props.resource
		if (res == null || res.path == '') return ''
		// #ifdef APP-HARMONY
		if (!res.path.startsWith('http://') && !res.path.startsWith('https://')) {
			return '/static/pdf/pdfjs-viewer.html'
		}
		// #endif
		const encodedSrc = encodeURIComponent(res.path)
		return '/static/pdf/pdfjs-viewer.html?file=' + encodedSrc + '#?file=' + encodedSrc
	})

	const onPdfWebViewLoad = () => {
		// #ifdef APP-HARMONY
		const res = props.resource
		if (res == null || res.path == '') return
		if (res.path.startsWith('http://') || res.path.startsWith('https://')) return
		
		try {
			const fs = uni.getFileSystemManager()
			const base64Str = fs.readFileSync(res.path, 'base64') as string
			// 修正: 传入第二个参数 component instance 以解决 cannot read property vm of undefined
			const instance = getCurrentInstance()
			const wvContext = uni.createWebviewContext(webviewId, instance?.proxy)
			wvContext.evalJS(`window.loadPdfFromBase64('${base64Str}')`)
		} catch (err: any) {
			console.error("读取 PDF 文件转 Base64 失败: ", err)
			emit('error', '加载鸿蒙本地PDF失败')
		}
		// #endif
	}

	const cadWebViewSrc = computed((): string => {
		const res = props.resource
		if (res == null || res.path == '') return ''
		// #ifdef APP-HARMONY
		if (!res.path.startsWith('http://') && !res.path.startsWith('https://')) {
			return '/static/cad/cad-launcher.html'
		}
		// #endif
		const encodedSrc = encodeURIComponent(res.path)
		return '/static/cad/cad-launcher.html?file=' + encodedSrc + '#?file=' + encodedSrc
	})

	const onCadWebViewLoad = () => {
		// #ifdef APP-HARMONY
		const res = props.resource
		if (res == null || res.path == '') return
		if (res.path.startsWith('http://') || res.path.startsWith('https://')) return
		
		try {
			const fs = uni.getFileSystemManager()
			const base64Str = fs.readFileSync(res.path, 'base64') as string
			const instance = getCurrentInstance()
			const wvContext = uni.createWebviewContext(cadWebviewId, instance?.proxy)
			wvContext.evalJS(`window.loadCadFromBase64('${base64Str}')`)
		} catch (err: any) {
			console.error("读取 CAD 文件转 Base64 失败: ", err)
			emit('error', '加载鸿蒙本地CAD失败')
		}
		// #endif
	}

	const docWebViewSrc = computed((): string => {
		const res = props.resource
		if (res == null || res.path == '') return ''
		// #ifdef APP-HARMONY
		if (!res.path.startsWith('http://') && !res.path.startsWith('https://')) {
			return '/static/ranuts-document/index.html'
		}
		// #endif
		const encodedSrc = encodeURIComponent(res.path)
		const encodedName = encodeURIComponent(res.name)
		return '/static/ranuts-document/index.html?src=' + encodedSrc + '&name=' + encodedName
	})

	const onDocWebViewLoad = () => {
		// #ifdef APP-HARMONY
		const res = props.resource
		if (res == null || res.path == '') return
		if (res.path.startsWith('http://') || res.path.startsWith('https://')) return
		
		try {
			const fs = uni.getFileSystemManager()
			const base64Str = fs.readFileSync(res.path, 'base64') as string
			const instance = getCurrentInstance()
			const wvContext = uni.createWebviewContext(docWebviewId, instance?.proxy)
			wvContext.evalJS(`if(window.openDocumentFromBase64){ window.openDocumentFromBase64('${base64Str}', '${res.name}') }`)
		} catch (err: any) {
			console.error("读取文档文件转 Base64 失败: ", err)
			emit('error', '加载鸿蒙本地文档失败')
		}
		// #endif
	}
	// #endif

	const isPlaying = ref(false)
	const currentTime = ref(0)
	const audioContext = ref<any | null>(null)
	
	const progressPercent = computed((): number => {
		const res = props.resource
		if (res == null || res.duration <= 0) return 0
		return (currentTime.value / res.duration) * 100
	})
	
	function isPdf(): boolean {
		const res = props.resource
		if (res == null) return false
		const parts = res.name.toLowerCase().split('.')
		if (parts.length < 2) return false
		return parts[parts.length - 1] == 'pdf'
	}
	
	// #ifdef WEB
	// PDF预览容器引用
	const pdfFrame = ref<UniElement | null>(null)
	// CAD预览容器引用
	const cadFrame = ref<UniElement | null>(null)
	// DOC预览容器引用
	const docFrame = ref<UniElement | null>(null)
	
	function renderPdfIframe(src : string): void {
		nextTick(() => {
			const el = pdfFrame.value as any
			if (el != null) {
				const encodedSrc = encodeURIComponent(src)
				el.innerHTML = '<iframe src="./static/pdf/pdfjs-viewer.html?file=' + encodedSrc + '#?file=' + encodedSrc + '" style="width:100%;height:100%;border:none;" frameborder="0"></iframe>'
			}
		})
	}
	
	function renderCadIframe(src : string): void {
		console.log('Rendering CAD file iframe with URL:', src)
		if (cadFrame.value == null) {
			console.warn('CAD frame container is not ready, retrying...')
			setTimeout(() => { renderCadIframe(src) }, 200)
			return
		}
		nextTick(() => {
			try {
				const el = (cadFrame.value as any).$el || (cadFrame.value as any)
				if (el != null) {
					const encodedSrc = encodeURIComponent(src)
					// 恢复使用 cad-launcher.html 中间层，利用其内部的 baseUrl 注入来修复 blob 异常
					el.innerHTML = '<iframe src="./static/cad/cad-launcher.html?file=' + encodedSrc + '#?file=' + encodedSrc + '" style="width:100%;height:100%;min-height:500px;border:none;" frameborder="0" allowfullscreen></iframe>'
					console.log('successfully injected iframe via cad-launcher.html')
				}
			} catch (e) {
				console.error('Failed to inject CAD iframe:', e)
			}
		})
	}

	function renderDocIframe(src : string): void {
		nextTick(async () => {
			const el = docFrame.value as any
			if (el != null) {
				const encodedName = encodeURIComponent(props.resource?.name ?? 'document')
				
				// 针对 Web 端的 blob: 跨域问题，如果为 blob，我们将先拉取并在父窗口转 base64
				let base64Str = ''
				console.log('[Parent] Preparing doc viewer for src:', src)
				if (src.startsWith('blob:')) {
					try {
						const response = await fetch(src)
						if (response == null) {
							throw new Error('fetch returned null response')
						}
						const blob = await response.blob()
						console.log('[Parent] Blob fetched, size:', blob.size)
						base64Str = await new Promise<string>((resolve) => {
							const reader = new FileReader()
							reader.onloadend = () => {
								const result = reader.result as string
								resolve(result.substring(result.indexOf(',') + 1))
							}
							reader.readAsDataURL(blob)
						})
						console.log('[Parent] Base64 converted, length:', base64Str.length)
					} catch (e) {
						console.error('[Parent] Failed to convert blob to base64', e)
					}
				}
				
				let docUrl = src
				if (base64Str === '') {
					// 只有非 blob 或者读取失败时，才走 src 传参模式
					if (!src.startsWith('http://') && !src.startsWith('https://') && !src.startsWith('blob:')) {
						docUrl = window.location.origin + (src.startsWith('/') ? '' : '/') + src
					}
				}
				
				const encodedSrc = encodeURIComponent(docUrl)
				
				// 确定 ranuts-document 的访问 URL 和 Electron 上下文
				let targetUrl = './static/ranuts-document/index.html'
				let electronPort = 0
				const eApi = (window as any)['electronAPI']
				
				if (process.env.NODE_ENV === 'development') {
					// 开发环境：使用独立静态服务器规避 Vite 崩溃问题
					targetUrl = 'http://localhost:5163/index.html'
				} else if (eApi != null) {
					// 生产环境：检测 Electron，使用主进程内嵌的 HTTP 服务器
					try {
						const port = await (eApi['getRanutsDocPort'] as (() => Promise<number>))()
						if (port > 0) {
							electronPort = port
							targetUrl = 'http://127.0.0.1:' + port + '/index.html'
							console.log('[Doc] 使用 Electron 内嵌 HTTP 服务:', targetUrl)
						}
					} catch (e) {
						console.warn('[Doc] 获取 ranuts-doc 端口失败，回退到相对路径', e)
					}
				}
				
				if (base64Str !== '' && eApi != null && electronPort > 0) {
					// Electron 环境：将文档保存为临时文件，通过 HTTP URL 加载
					// 这样 OnlyOffice 使用短 HTTP URL 而非将 3.7MB data URI 嵌入 URL 参数
					try {
						const saveFn = eApi['saveTempDoc'] as ((b64: string, name: string) => Promise<string>)
						const docPath = await saveFn(base64Str, props.resource?.name ?? 'document')
						const docHttpUrl = 'http://127.0.0.1:' + electronPort + docPath
						console.log('[Doc] 文档已保存为临时文件，HTTP URL:', docHttpUrl)
						el.innerHTML = '<iframe src="' + targetUrl + '?src=' + encodeURIComponent(docHttpUrl) + '&name=' + encodedName + '" style="width:100%;height:100%;border:none;" frameborder="0"></iframe>'
					} catch (e) {
						console.error('[Doc] 保存临时文档失败:', e)
					}
				} else if (base64Str !== '') {
					// 非 Electron Web 环境：通过 postMessage 发送 base64
					const cacheBusterUrl = targetUrl + '?t=' + Date.now()
					el.innerHTML = '<iframe id="ranuts-iframe" src="' + cacheBusterUrl + '" style="width:100%;height:100%;border:none;" frameborder="0"></iframe>'
					
					let attempts = 0
					const intervalId = setInterval(() => {
						attempts++
						const iframe = el.querySelector('#ranuts-iframe') as any
						if (iframe && iframe.contentWindow) {
							console.log(`[Parent] Sending loadDocumentBase64 message (attempt ${attempts})...`);
							iframe.contentWindow.postMessage({
								type: 'loadDocumentBase64',
								base64: base64Str,
								name: props.resource?.name ?? 'document'
							}, '*')
						}
						if (attempts >= 10) {
							clearInterval(intervalId)
						}
					}, 500)
					
					const ackListener = (event: MessageEvent) => {
						if (event.data && event.data.type === 'documentLoadedACK') {
							console.log('[Parent] Received ACK from iframe, clearing interval.')
							clearInterval(intervalId)
							window.removeEventListener('message', ackListener)
						}
					}
					window.addEventListener('message', ackListener)
				} else {
					el.innerHTML = '<iframe src="' + targetUrl + '?src=' + encodedSrc + '&name=' + encodedName + '" style="width:100%;height:100%;border:none;" frameborder="0"></iframe>'
				}
			}
		})
	}
	
	// 监听资源变化，渲染PDF
	watch(() => props.resource, (newVal : ResourceItem | null) => {
		if (newVal != null && isPdf()) {
			renderPdfIframe(newVal.path)
		}
		if (newVal != null && newVal.type == 'cad') {
			renderCadIframe(newVal.path)
		}
		if (newVal != null && newVal.type == 'document') {
			renderDocIframe(newVal.path)
		}
	}, { immediate: true })
	// #endif
	
	// 格式化时间
	const formatTime = (seconds: number): string => {
		return formatDuration(seconds)
	}
	
	// 图片加载错误
	const onImageError = (e: UniImageErrorEvent) => {
		console.error('Image load error:', e)
		emit('error', '图片加载失败')
	}
	
	// 视频加载错误
	const onVideoError = (e: UniVideoErrorEvent) => {
		console.error('Video load error:', e)
		emit('error', '视频加载失败')
	}
	
	// 视频时间更新
	const onTimeUpdate = (e: UniVideoTimeUpdateEvent) => {
		currentTime.value = e.detail.currentTime
	}
	
	// 播放结束
	const onEnded = () => {
		isPlaying.value = false
		currentTime.value = 0
	}
	
	const toggleAudio = (): void => {
		const res = props.resource
		if (res == null) return
		
		// #ifdef WEB
		if (audioContext.value == null) {
			const audio = new Audio(res.path)
			audio.ontimeupdate = () => {
				currentTime.value = audio.currentTime
			}
			audio.onended = () => {
				isPlaying.value = false
				currentTime.value = 0
			}
			audioContext.value = audio
		}
		
		const ctx = audioContext.value as any
		if (isPlaying.value) {
			(ctx as UTSJSONObject)['pause']!()
		} else {
			(ctx as UTSJSONObject)['play']!()
		}
		isPlaying.value = !isPlaying.value
		// #endif
		
		// #ifndef WEB
		// App 端使用 uni.createInnerAudioContext
		if (audioContext.value == null) {
			const innerCtx = uni.createInnerAudioContext()
			innerCtx.src = res.path
			innerCtx.onTimeUpdate(() => {
				currentTime.value = innerCtx.currentTime
			})
			innerCtx.onEnded(() => {
				isPlaying.value = false
				currentTime.value = 0
			})
			audioContext.value = innerCtx
		}
		
		const appCtx = audioContext.value as InnerAudioContext
		if (isPlaying.value) {
			appCtx.pause()
		} else {
			appCtx.play()
		}
		isPlaying.value = !isPlaying.value
		// #endif
	}
	
	const openDocument = (): void => {
		const res = props.resource
		if (res == null) return
		
		// #ifdef APP-ANDROID || APP-HARMONY
		uni.openDocument({
			filePath: res.path,
			fail: (err: any) => {
				const errMsg = err instanceof Error ? err.message : (err != null ? err.toString() : 'unknown');
				uni.showToast({
					title: '无法打开文档: ' + errMsg,
					icon: 'none'
				})
			}
		})
		// #endif
		
		// #ifdef WEB
		window.open(res.path, '_blank')
		// #endif
	}
	
	// 监听资源变化，重置状态
	watch(() => props.resource, (newVal: ResourceItem | null) => {
		// 停止当前播放
		if (audioContext.value != null) {
			const ctx = audioContext.value as any
			// #ifdef WEB
			(ctx as UTSJSONObject)['pause']!()
			audioContext.value = null
			// #endif
			
			// #ifndef WEB
			const innerCtx = ctx as InnerAudioContext
			innerCtx.stop()
			innerCtx.destroy()
			audioContext.value = null
			// #endif
		}
		
		isPlaying.value = false
		currentTime.value = 0
	})
	
	// 组件卸载时清理
	onUnmounted(() => {
		if (audioContext.value != null) {
			const ctx = audioContext.value as any
			// #ifdef WEB
			try {
				(ctx as UTSJSONObject)['pause']!()
			} catch (e: any) {}
			// #endif
			
			// #ifndef WEB
			const innerCtx = ctx as InnerAudioContext
			innerCtx.stop()
			innerCtx.destroy()
			// #endif
		}
	})
</script>

<style>
	/* Cloud Atlas 云图白主题 — 资源预览 */
	.resource-preview {
		flex: 1;
			flex-direction: column;
		background-color: #FFFFFF;
		border-radius: 8px;
		overflow: hidden;
		border-width: 1px;
		border-style: solid;
		border-color: #E2E6ED;
		padding: 2px;
		margin-top:4px
	}
	
	/* 空状态 */
	.empty-state {
		flex: 1;
			flex-direction: column;
		justify-content: center;
		align-items: center;
	}
	
	.empty-icon {
		font-size: 48px;
		margin-bottom: 12px;
	}
	
	.empty-text {
		font-size: 14px;
		color: #9CA3AF;
	}
	
	/* 预览容器 */
	.preview-container {
		flex: 1;
			flex-direction: column;
		padding: 4px;
	}
	
	/* 图片预览 */
	.image-preview {
		justify-content: center;
		align-items: center;
	}
	
	.preview-image {
		flex: 1;
		width: 100%;
		max-height: 300px;
		border-radius: 4px;
	}
	
	/* 视频预览 */
	.video-preview {
		justify-content: flex-start;
	}
	
	.preview-video {
		width: 100%;
		height: 200px;
		border-radius: 4px;
		background-color: #111827;
	}
	
	/* 音频预览 */
	.audio-preview {
		justify-content: center;
		align-items: center;
	}
	
	.audio-visual {
			flex-direction: column;
		align-items: center;
		margin-bottom: 20px;
	}
	
	.audio-icon {
		font-size: 64px;
		margin-bottom: 16px;
	}
	
	.audio-wave {
			flex-direction: row;
		align-items: flex-end;
		height: 40px;
	}
	
	.wave-bar {
		width: 6px;
		height: 20px;
		background-color: #0891B2;
		margin: 0 3px;
		border-radius: 3px;
	}
	
	.wave-bar.playing {
		background-color: #059669;
	}
	
	.audio-controls {
		width: 100%;
			flex-direction: row;
		align-items: center;
		padding: 0 20px;
	}
	
	.audio-btn {
		width: 48px;
		height: 48px;
		border-radius: 24px;
		background-color: #0891B2;
			justify-content: center;
		align-items: center;
		margin-right: 16px;
	}
	
	.audio-progress {
		flex: 1;
			flex-direction: column;
	}
	
	.progress-bar {
		height: 6px;
		background-color: #E2E6ED;
		border-radius: 3px;
		overflow: hidden;
	}
	
	.progress-fill {
		height: 100%;
		background-color: #0891B2;
		border-radius: 3px;
	}
	
	.time-text {
		font-size: 12px;
		color: #6B7280;
		margin-top: 4px;
	}
	
	/* PDF预览 */
	.pdf-preview {
		flex-direction: column;
		height: 100%;
	}
	
	.pdf-frame-container {
		flex: 1;
		width: 100%;
	}
	
	/* 文档预览 */
	.document-preview {
		justify-content: center;
		align-items: center;
		height: 100%;
	}
	
	.document-icon-wrap {
		margin-bottom: 16px;
	}
	
	.document-icon {
		font-size: 64px;
	}
	
	.resource-hint {
		font-size: 12px;
		color: #9CA3AF;
		margin-top: 4px;
	}
	
	.open-btn {
		margin-top: 20px;
		padding: 12px 32px;
		background-color: #0891B2;
		border-radius: 8px;
	}
	
	.btn-text {
		font-size: 14px;
		color: #FFFFFF;
	}
	
	/* 未知类型 */
	.unknown-preview {
		justify-content: center;
		align-items: center;
	}
	
	.unknown-icon {
		font-size: 48px;
		margin-bottom: 12px;
	}
	
	.unknown-text {
		font-size: 14px;
		color: #9CA3AF;
		margin-bottom: 8px;
	}
	
	/* 资源信息 */
	.resource-info {
		margin-top: 4px;
		padding: 4px;
			flex-direction: column;
		align-items: center;
	}
	
	.resource-name {
		font-size: 14px;
		color: #111827;
		font-weight: normal;
	}
	
	.resource-desc {
		font-size: 12px;
		color: #6B7280;
		margin-top: 4px;
	}
	
	.resource-duration {
		font-size: 12px;
		color: #0891B2;
		margin-top: 4px;
	}
	
	/* CAD 图纸预览 — 无 padding，最大化显示 */
	.cad-preview-container {
		flex: 1;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		width: 100%;
		height: 100%;
		background-color: #F0F2F5;
		overflow: hidden;
	}
	
	.cad-frame-container {
		flex: 1;
		width: 100%;
	}
	
	.cad-webview {
		flex: 1;
		width: 100%;
		background-color: #F0F2F5;
	}
	
	/* CAD 底部信息栏 — 极简紧凑 */
	.cad-info {
		padding: 4px 8px;
		flex-direction: row;
		align-items: center;
		justify-content: center;
		background-color: #F8FAFC;
		border-top-width: 1px;
		border-top-style: solid;
		border-top-color: #E2E6ED;
	}
	
	.cad-info-name {
		font-size: 12px;
		color: #6B7280;
	}
	
	.pdf-webview {
		flex: 1;
		width: 100%;
		background-color: #FFFFFF;
	}

	.pdf-native-view {
		flex: 1;
		width: 100%;
		background-color: #F3F4F6;
	}
	
	.cad-label {
		color: #0891B2;
		font-weight: normal;
	}
</style>
